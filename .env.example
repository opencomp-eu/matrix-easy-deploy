# matrix-easy-deploy — example environment file
# Copy to .env and fill in — setup.sh does this automatically.
# Never commit .env to version control.

# The public domain your Matrix server is reachable on
MATRIX_DOMAIN=matrix.example.com

# The Matrix "server name" — appears in user IDs like @user:SERVER_NAME
# Usually your base domain, not the matrix subdomain
SERVER_NAME=example.com

# Admin account username
ADMIN_USERNAME=admin

# PostgreSQL credentials (auto-generated by setup.sh)
POSTGRES_PASSWORD=changeme

# Synapse internal secrets (auto-generated by setup.sh)
REGISTRATION_SHARED_SECRET=changeme
MACAROON_SECRET_KEY=changeme
FORM_SECRET=changeme

# Whether to allow public user registration (true/false)
ENABLE_REGISTRATION=false

# Whether Element web client is installed (true/false)
# Affects which Caddyfile template is used and which Docker Compose profile is active
INSTALL_ELEMENT=true

# The domain Element is served on (only used when INSTALL_ELEMENT=true)
ELEMENT_DOMAIN=element.example.com
